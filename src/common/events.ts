const LOGOUT = 'logout';
const POST_LOGOUT = `post-${LOGOUT}`;
const REGISTER = 'register';
const POST_REGISTER = `post-${REGISTER}`;
const EXECUTE_SCRIPT = 'execute-script';
const POST_EXECUTE_SCRIPT = `post-${EXECUTE_SCRIPT}`;
const CONNECT = 'connect';
const DISCONNECT = 'disconnect';
const GET_CLIENTS = 'get-clients';
const POST_GET_CLIENTS = `post-${GET_CLIENTS}`;
const NEW_CLIENT_REGISTERED = 'new-client-registered';
const CLIENT_DISCONNECTED = 'client-disconnected';
const GET_CLIENT_BY_ID = 'get-client-by-id';
const POST_GET_CLIENT_BY_ID = `post-${GET_CLIENT_BY_ID}`;

export {
  LOGOUT,
  POST_LOGOUT,
  REGISTER,
  POST_REGISTER,
  EXECUTE_SCRIPT ,
  POST_EXECUTE_SCRIPT,
  CONNECT,
  DISCONNECT,
  GET_CLIENTS,
  GET_CLIENT_BY_ID,
  POST_GET_CLIENT_BY_ID,
  POST_GET_CLIENTS,
  NEW_CLIENT_REGISTERED,
  CLIENT_DISCONNECTED
};
